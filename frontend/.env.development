# Frontend Development Environment Variables
# This configures the frontend dev server to use the backend API running in Docker

# Backend API URL (when running backend in Docker on port 5000)
VITE_API_BASE_URL=http://localhost:5000

# Note: Restart the dev server after changing environment variables
# npm run dev must be restarted to pick up changes to this file
